
#comanda add menu

categorias ::: menu().currentSubCategorias()
    css: {'sin-imagen': !image_url, 'con-imagen': image_url}
    visible: image_url, attr: {src: urlDomain+'img/menu/'+image_url}
    text: name

boton:::: menu().path()
    attr: {  'data-icon': esUltimoDelPath()?'':'back', 
             'css': {'ui-btn-active': esUltimoDelPath()}
             }, 
    click: seleccionar

productos ::: menu().currentProductos()
    "attr: { href: tieneSabores() ? '#page-sabores' : '#'}, css: {'producto-con-sabor': tieneSabores()}
    text: name

sabores ::: adn().currentSabores()
    text: name
click: function(){adn().currentMesa().currentComanda().saveSabores()}

prouctos seleccionados:::: adn().productosSeleccionados()
    visible: cant(), css:{'es-entrada': esEntrada(), 'tiene-observacion': observacion()}
    click: seleccionar
    click: deseleccionar
    click: addObservacion, style: { background: observacion() ? '#437FBE' : ''}
    click: toggleEsEntrada, css: { es_entrada: esEntrada()}
    text: realCant()
    text: nameConSabores() + ' ' +observacion()

Header Mesa
    text: adn().currentMesa().numero()
    text: adn().currentMesa().mozo().numero()
    text: adn().currentMesa().getEstadoName()


comanda
    value: adn().currentMesa().currentComanda().comanda.observacion, valueUpdate: 'keyup'
    


$listado-mesas
header ->    adn().mesas().length

    TMP: listaMesas ::: adn().mesas
    attr: {mozo: mozo().id(), 'id': 'mesa-li-id-'+id(), 'class': estado().icon}
    click: seleccionar, attr: {accesskey: numero, id: 'mesa-id-'+id()}
    text: numero
    text: mozo().numero
    visible: clienteDescuentoText(),text: clienteDescuentoText()
    visible: clienteTipoFacturaText()
    text: clienteTipoFacturaText()
    text: textoHora()












-----------------------------------------------------------------------------------
categorias
	image_url
	name
	tieneSabores
	menu().path()
	menu().currentSubCategorias()
	menu().currentProductos()
	adn().currentMesa().currentComanda().saveSabores()
	adn().currentSabores()
        esUltimoDelPath


comanda-add
	timeCreated()
        id
        productsStringListing()
        imprimirComanda
        


productos-seleccionados
        currentComanda.DetalleComanda // para listar los productos seleccionados
	adn().productosSeleccionados()
	cant
	esEntrada
	obsevacion
	seleccionar
	deseleccionar
	addObservacion
	toggleEsEntrada
	realCant
	nameConSabores
	deseleccionarYEnviar        

productos
	name
        precio

mesas abiertas
     Math.abs(adn().mesasCerradas().length - adn().mesas().length)

mesas cerradas
    adn().mesasCerradas 


adn().vueltoText()

mesa
    adn().mesas
        adn().currentMesa().id
	adn().currentMesa().numero()
	adn().currentMesa().mozo().numero()
	adn().currentMesa().getEstadoName()
	adn().currentMesa().currentComanda().comandaSettings.imprimir( 0 )
	adn().currentMesa().currentComanda().comanda.observacion
        adn().currentMesa().total()
        estado().icon
        clienteDescuentoText()
        clienteTipoFacturaText
        totalCalculado
        textoHora
        seleccionar
        adn().mesas().length
        adn().currentMesa().cant_comensales

pagos
        image(), 
        TipoDePago().name
        valor
        adn().pagos
        

mozo
        mozo().numero



#mesa_menu
    adn().currentMesa().menu